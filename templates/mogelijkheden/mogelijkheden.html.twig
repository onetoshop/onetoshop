    {% extends 'base1.html.twig' %}

{% block body %}
{#    {% for message in app.flashes('succes') %}#}
{#        <div class="alert alert-success"#}
{#        {{ message }}#}
{#        </div>#}
{#    {% endfor %}#}
    <div class="site-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-12 text-center">
                <h3 class="section-sub-title">Aanmelden</h3>
                    <h2 class="section-title ">Klaar voor uw webshop?</h2>
                </div>
            </div>

            <div class="row justify-content-center">
                <div class="col-md-7 mb-5">


                    <div class="p-3 bg-white">

                        {{ form_start(form) }}
                        <div class="row form-group">
                            <div class="col-md-6 mb-3 mb-md-0">
                        {{ form_row(form.voornaam) }}
                            </div>
                            <div class="col-md-6">
                        {{ form_row(form.achternaam) }}
                            </div>
                        </div>
                            {{ form_row(form.telefoon) }}
                            {{ form_row(form.email) }}
                            {{ form_row(form.doel) }}
                            {{ form_row(form.voorkeur) }}
                            {{ form_row(form.contact) }}
                        <div class="text-center">
                            <input type="submit" style="width: 100%;" class="btn btn-black btn-outline-black py-3 px-5 rounded-0 mb-lg-0 mb-2 d-block d-sm-inline-block" value="AANMELDEN">
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>

        </div>
    </div>
    <script>
        var confirmOnPageExit = function (e)
        {
            // If we haven't been passed the event get the window.event
            e = e || window.event;

            var message = 'Any text will block the navigation and display a prompt';

            // For IE6-8 and Firefox prior to version 4
            if (e)
            {
                e.returnValue = message;
            }

            // For Chrome, Safari, IE8+ and Opera 12+
            return message;
        };
    </script>
{% endblock %}
