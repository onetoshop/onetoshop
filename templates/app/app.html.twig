{% extends 'base1.html.twig' %}
{% block title %}<title>Uitgebreide keuze uit koppelingen met bestaande Apps | Onetoshop</title>{% endblock %}
{% block navbar %}

    <div class="col-12 col-md-10 d-none d-xl-block">
        <nav class="site-navigation position-relative text-right" role="navigation">

            <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                <a href="{{ path('home') }}" class="nav-link">Home</a>
                <a href="{{ path('functionaliteit') }}" class="nav-link">Functonaliteit</a>
                <a href="{{ path('apps') }}" class="nav-link active">Apps</a>
                <a href="{{ path('blogs') }}" class="nav-link ">Blogs</a>
            </ul>
        </nav>
    </div>

{% endblock %}
{% block body %}
    <div class="" id="contact-section">
    <!-- Page Content -->
    <div class="container">
        <!-- Heading Row -->
        <div class="row align-items-center my-5">
            <!-- /.col-lg-8 -->
            <div class="col-12">
                <h1>Welke Apps kies jij?</h1>
                <p class="font-weight-light" style="margin-top: 10px;"> Apps kunnen aan je webshop gekoppeld worden om een aantal zaken te vereenvoudigen. Door middel van deze koppelingen kun je, bijvoorbeeld, het betaalproces en verzendproces automatiseren, snel en gemakkelijk e-mails en nieuwsbrieven versturen naar je klanten, je nieuws en producten delen op sociale media en feedback van klanten op je webshop weergeven.</p>
            </div>
            <!-- /.col-md-4 -->
        </div>
        <!-- /.row -->
        <!-- Content Row -->

        <div class="row">
            {% if apps %}
                {% for apps in apps %}
                <div class="col-md-4 mb-5">
                    <div class="card h-100">
                        <div class="card-body">
                            <h3 class="card-title">{{ apps.naam | raw }}</h3>
                            <p class="card-text">{{ apps.body | raw }}</p>
                        </div>
                        <div class="card-footer">
                            <a style="width: 100%;" href="{{ path('appinfo', {'slug' : apps.url}) }}"  class="btn btn-outline-primary">Lees Verder</a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            {% else %}
                <p>Er Zijn Geen Gegevens Gevonden!</p>
            {% endif %}
            <!-- /.col-md-4 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container -->
</div>
{% endblock %}
