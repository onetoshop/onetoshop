{% extends 'admin/adminbase.html.twig' %}

{% block body %}
    <div id="content-wrapper">

    <div class="container-fluid">

    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="{{ path('admin') }}">Dashboard</a>
        </li>
        <li class="breadcrumb-item active">
            <a href="{{ path('functionaliteitadmin') }}">Functionaliteit Overzicht</a>
        </li>
        <li class="breadcrumb-item active">Functionaliteit Toevoegen</li>
    </ol>
    <!-- Page Content -->
    <h1>Functionaliteit Toevoegen</h1>
    <!-- DataTables Example -->
    <div class="form-element-area">
        <div class="container">
            <div id="row">
                <div class="col-xs-12">
                    <div class="form-element-list">
                        <div class="basic-tb-hd">
                            <div class="cmp-tb-hd">
                                <div class="cmp-tb-hd">
                                    <div>
                                        {{ form_start(form) }}

                                        <p><h3>Naam</h3></p>
                                        <p class="font-weight-light">Voer hier de naam van de nieuwe functionaliet</p>
                                        {% form_theme form.name 'bootstrap_4_layout.html.twig' %}
                                        {{ form_row(form.name) }}

                                        <p><h3>Beschrijving</h3></p>
                                        <p class="font-weight-light">Voer hier de beschrijving die bij de functionaliet hoord.</p>
                                        {{ form_row(form.body) }}

{#                                        {{ form_row(form.slug) }}#}
                                        <hr>
                                        <button type="submit" style="width: 100%;" class="btn btn-primary">koppeling toevoegen</button>
                                        {{ form_end(form) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}