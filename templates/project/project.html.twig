{% extends 'base1.html.twig' %}
{% block navbar %}

    <div class="col-12 col-md-10 d-none d-xl-block">
        <nav class="site-navigation position-relative text-right" role="navigation">

            <ul class="site-menu main-menu js-clone-nav mr-auto d-none d-lg-block">
                <a href="{{ path('home') }}" class="nav-link">Home</a>
                <a href="{{ path('functionaliteit') }}" class="nav-link">Functonaliteit</a>
                <a href="{{ path('apps') }}" class="nav-link">Apps</a>
                <a href="{{ path('blogs') }}" class="nav-link ">Blogs</a>
            </ul>
        </nav>
    </div>

{% endblock %}
{% block body %}
    <div id="contact-section">
        <div class="container">
            <!-- Heading Row -->
            {% for project in project %}
                <div class="row align-items-center my-5">
                    <div class="col-lg-5">
                        <img class="img-fluid rounded mb-4 mb-lg-0" src="{{ asset('/image/project/' ~ project.image.name) }}" alt="">
                    </div>
                    <!-- /.col-lg-8 -->
                    <div class="col-lg-7">
                        <h1 class="font-weight-light">{{ project.title | raw }}</h1>
                        <p>{{ project.body | raw }}</p>
                    </div>
                    <!-- /.col-md-4 -->
                </div>
            {% endfor %}
            <hr>
            <!-- /.row -->
            <!-- Content Row -->
            <div class="row">
                <div class="col-lg-6">
                    <h1 class="font-weight-light">Onetoshop proberen?</h1>
                    <p>Wil je rustig ontdekken wat Onetoshop allemaal te bieden heeft? Probeer het platform dan geheel vrijblijvend 30 dagen lang uit.</p>
                    <a class="btn btn-primary" href="{{ path('succes') }}">Demo aanvragen</a>
                </div>
                <div class="col-lg-6">
                    <h1 class="font-weight-light">Welke apps kies jij?</h1>
                    <p>Bij Onetoshop integreren we gemakkelijk jouw webshop met je favoriete betaal-, verzend en social media apps.
                        Éénvoudig te beheren via het gebruiksvriendelijk ingerichte Onetoshop dashboard.
                        Welke apps kies jij?</p>
                </div>
                <!-- /.col-md-4 -->
            </div>
            <!-- /.row -->
            <hr>
            <div>
                <a onclick="goBack()"><button  class="back-button" type="button">Klik om terug te gaan</button></a>
            </div>
            <script>
                function goBack() {
                    window.history.back();
                }
            </script>
        </div>
        <!-- /.container -->
    </div>
{% endblock %}