{% extends 'base.html.twig' %}

{% block title %}show image!{% endblock title%}

{% block body %}
    {% for image in image %}
        <img src="{{ asset('/public/image/' ~ image.image) }}"/>
{#        {{ dump(image) }}#}
    {% endfor %}
{% endblock body%}
